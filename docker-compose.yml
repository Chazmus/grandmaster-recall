services:
  grandmaster-recall:
    build:
      context: .
      dockerfile: Dockerfile
    image: ghcr.io/chazmus/grandmaster-recall:latest
    container_name: grandmaster-recall
    restart: unless-stopped
    volumes:
      - ./data:/app/data
    networks:
      - proxy-net

networks:
  proxy-net:
    external: true
